# Rust (backend/)
/target
/backend/target
*.db
*.db-wal
*.db-shm
.env

# UniFFI 生成产物（由 uniffi generate 生成）
/backend/bindings

# 本地自签名 TLS 证书（由 backend/scripts/gen-cert.ps1 生成）
/backend/certs

# 一键启动脚本产生的后端日志
.dev-server.log

# 微信 iLink 扫码凭证（ Bearer token，密钥）
/backend/ilink_session.json

# 编辑器
.vscode/
.idea/

# Web 前端 (frontend/)
frontend/node_modules/
frontend/dist/
*.tsbuildinfo

# Android (android/)
android/.gradle/
android/.idea/
android/build/
android/app/build/
android/local.properties